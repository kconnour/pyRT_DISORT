import setuptools

setuptools.setup(
    name='pyRT_DISORT',
    version='0.0.1',
    author='Kyle Connour',
    description='This does RT',
    packages=setuptools.find_packages(),
    include_package_data=True,
    python_requires='>=3.8'
)
